import { FormContainer, LoginForm } from '@/components'

const LoginPage = () => {
  return (
    <FormContainer title="Login">
      <LoginForm />
    </FormContainer>
  )
}

export default LoginPage
